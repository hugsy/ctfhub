# Core packages
django >= 4.2.2
Pillow
requests
python-magic
psycopg2-binary
django-model-utils
django-sendfile2
bleach

# Dev packages (install those if you intend to submit PRs)
pre-commit
black

# Test packages (for running tests)
pytest
pytest-django
